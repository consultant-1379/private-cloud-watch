# Set these variables if they aren't already set.
: "${TEST_NAME:="crux"}"
: "${DOCKER_IMAGE:="erixzone/crux-main"}"
: "${CLUSTER_SIZE:=3}"
: "${REPLICAS:=12}"
: "${DIGITALOCEAN_TOKEN:="big_long_token_goes_here"}"
: "${REMOVE_PREEXISTING_NODES:="yes"}"
