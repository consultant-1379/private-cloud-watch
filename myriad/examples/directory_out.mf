version = "v0.2"

job "directory_out" {
    command = <<EOF
sh -c "mkdir -p junk; uname -a > junk/test.txt; cat junk/test.txt; ls -alt > junk/ls.txt"
EOF
    output "/junk" {
        dst = "oot"
    }
}
